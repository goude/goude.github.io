---
import PaperLayout from "@/layouts/PaperLayout.astro";
import NoteCard from "@/components/NoteCard.astro";

interface Meta {
  title: string;
  description: string;
  date: string;
}

const modules = import.meta.glob<{ frontmatter: Meta }>("./*.{md,astro}", {
  eager: true,
});

const entries = Object.entries(modules)
  .map(([path, mod]) => {
    const slug = path
      .split("/")
      .pop()!
      .replace(/\.(md|astro)$/, "");
    return { href: `/notes/${slug}`, ...mod.frontmatter };
  })
  .sort((a, b) => +new Date(b.date) - +new Date(a.date));
---

<PaperLayout
  title="Notes"
  description="Field notes — typed entries, longer than a jotting."
  current="notes"
>
  <header class="paper-section-header">
    <p class="paper-eyebrow">Section I</p>
    <h2 class="paper-h paper-h-section">Notes</h2>
  </header>

  <div class="paper-stack">
    {
      entries.map((e, i) => (
        <NoteCard
          title={e.title}
          href={e.href}
          date={e.date}
          tilt={i % 2 ? 1.2 : -1.2}
        >
          <p style="margin:0;">{e.description}</p>
          <p style="margin:14px 0 0;">
            <a href={e.href}>Open →</a>
          </p>
        </NoteCard>
      ))
    }
  </div>
</PaperLayout>
